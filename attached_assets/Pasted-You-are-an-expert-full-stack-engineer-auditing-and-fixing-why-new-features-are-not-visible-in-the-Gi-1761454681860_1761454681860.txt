You are an expert full-stack engineer auditing and fixing why new features are not visible in the GigifyPro app.

OBJECTIVE
Force the new UI/UX to appear by verifying route mounts, wiring client routes/links, disabling caches temporarily, rebuilding, and seeding minimal demo data. Then print a verification report with clickable URLs.

DO (IN ORDER) — REPORT EACH STEP BRIEFLY

1) BRANCH & FILE AUDIT
- Print current branch and last 3 commits: `git status -sb && git log --oneline -3`
- List existence of key files (print ✅/❌):
  server/routes/explore.ts
  server/routes/content.testimonials.ts
  server/routes/community.ts    (or gsquare)
  server/routes/admin.analytics.ts
  server/scripts/seedStore.ts
  server/scripts/seedGSquare.ts
  client/src/pages/Explore.tsx
  client/src/pages/Store.tsx
  client/src/pages/KnowledgeHub.tsx (or Knowledge.tsx)
  client/src/pages/community/CommunityHome.tsx (or GSquare.tsx)
  client/src/components/Navbar.tsx

2) SERVER MOUNTS
- Open server/routes.ts (or the central API router) and ensure these lines exist; ADD if missing:
  import explore from "./routes/explore";
  import testimonials from "./routes/content.testimonials";
  import community from "./routes/community";
  import adminAnalytics from "./routes/admin.analytics";
  api.use("/explore", explore);
  api.use("/content/testimonials", testimonials);
  api.use("/gsquare", community);
  api.use("/admin/analytics", adminAnalytics);
- Save file if changed.

3) TEMP DISABLE SERVER CACHE
- If server/utils/cache.ts exists, export a flag `export const CACHE_DISABLED = true;`
- In knowledge/testimonials routes: if CACHE_DISABLED, skip cache and set `Cache-Control: no-store`.
- If the flag file doesn’t exist, add a small module with:
  `export const CACHE_DISABLED = true;`
  and import it in those routes.

4) CLIENT ROUTES & NAVBAR
- Open client/src/components/Navbar.tsx and ensure links exist (edit if needed):
  Explore → "/explore"
  Store   → "/store"
  Knowledge → "/knowledge"
  G-Square → "/gsquare"
- If any link still points to "/feed", change to "/explore".
- Open client/src/App.tsx (or router) and ensure these routes are registered:
  /explore  → Explore component
  /store    → Store component
  /knowledge → KnowledgeHub (or current knowledge page)
  /gsquare  → Community/G-Square home
  /admin/analytics → AdminAnalytics (if exists)
- Save files if changed.

5) FORCE CLIENT REFRESH (VISIBLE VERSION TAG)
- Create or update client/src/components/BuildTag.tsx:
  ------------------------------------------------
  export default function BuildTag() {
    const ts = new Date().toLocaleString();
    return <div className="fixed bottom-2 right-2 rounded bg-black/70 text-white text-xs px-2 py-1">
      GP Build · {ts}
    </div>;
  }
  ------------------------------------------------
- Mount <BuildTag/> in the root layout (e.g., in App.tsx at the end of the JSX) so it shows on every page.
- This ensures you’ll SEE the rebuild timestamp.

6) TEMP DISABLE CLIENT MEMOIZED FETCH
- Open client/src/lib/api.ts (or equivalent). Set default TTL to 0 or bypass mem cache if `import.meta.env.VITE_DISABLE_CACHE === "1"`.
- Add `VITE_DISABLE_CACHE=1` to .env.example and (only if necessary) to Replit Secrets for this run.

7) SEED MINIMUM DEMO DATA (SAFE UPSERTS)
- Run:
  npx prisma generate
  npx prisma migrate dev -n "visible_sync"
- Execute seeds if present; if missing, create minimal ones that upsert:
  - store: chef-hat, chef-jacket, cleaning-apron, reflective-jacket, laundry-bag-kit with placeholder images in client/public/store/*.png (create neutral placeholders if missing).
  - gsquare: if 0 posts, insert 20 demo posts across last 60 days with mixed topics and a couple of images.
- Commands to run:
  npx ts-node server/scripts/seedStore.ts || true
  npx ts-node server/scripts/seedGSquare.ts || true

8) REBUILD & RESTART
- Kill and restart dev processes.
- Run client build if applicable: `npm run build` then `npm run dev`.

9) QUICK SANITY CALLS (PRINT RESPONSES)
- Print first 1–2 lines of each:
  GET /api/health  (add simple health route if missing)
  GET /api/content/testimonials
  GET /api/explore/top?take=5
  GET /api/admin/analytics/totals (if admin auth present)
- Note counts (products, posts).

10) VERIFICATION REPORT (PRINT THIS CLEARLY)
- Navbar links now present: Explore, Store, Knowledge, G-Square → ✅/❌
- Clickable page URLs to test:
  /explore
  /store
  /knowledge
  /gsquare
- Show the current BuildTag timestamp rendered on the page (confirm in console output).
- Show counts summary: { products, posts, services }.

NOTES
- Do not delete existing content.
- Use clear ✅/❌ markers so a non-developer can verify quickly.
- Keep CACHE_DISABLED and VITE_DISABLE_CACHE enabled only for this session; we’ll re-enable caching after visual verification.